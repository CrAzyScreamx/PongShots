name: Build and Publish Docker Image
on:
  push:
    paths:
      - Java/**

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Run Gradle Tests
        run: ./java/gradlew test